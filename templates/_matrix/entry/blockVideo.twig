{{ include('_shared/components/video', {
    sectionTitle: entry.sectionTitle,
    videoTitle: entry.title,
    videoSource: entry.videoSource,
    videoId: entry.videoId
}, with_context = false, ignore_missing = true) }}